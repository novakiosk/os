# CUPS usb:// queues and the agent's read-only Zebra status probe use USBFS.
# The legacy /dev/usb/lp driver otherwise claims idle printer interfaces until
# CUPS detaches it for the first print, blocking status probes after boot/replug.
blacklist usblp
